# first add paths to your .bashrc and .tcshrc files
# make these if they do not already exist, they reside in your home (~) dir

> ~/.bashrc
PATH=$PATH:/ifs/home/at570/disk1/Resources/Bin/linux:/ifs/home/kellys04/pipeline-master/code


# '/ifs/home/kellys04/pipeline-master/code' is the location of the pipeline repo

> ~/.tcshrc 
set path = ( \
        $path \
        ~/Bin/linux \
        ~/Code/pipelines-repo/code \
)

